---
title: Release Notes
description: A running log of notable features and changes shipped to Beacon.
---

# Release Notes

This page tracks notable features and changes as they ship, newest first.

## 2026-08-05

- Added this Release Notes page to track new features going forward.

- Added a GitHub webhook that triggers when a PR merges into `main` touching `content/docs/**/*.mdx` or `content/nav.config.ts`. It checks whether other doc pages are now stale and writes suggestions to Vercel Blob storage for manual review.
